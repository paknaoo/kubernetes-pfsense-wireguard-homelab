# metrics-server Notes

This directory documents the metrics-server configuration adjustment used in the Kubernetes lab.

metrics-server provides resource metrics for nodes and pods, enabling standard Kubernetes commands such as:

```bash
kubectl top nodes
kubectl top pods -A
